<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:gravity="center"
    android:orientation="horizontal"
    android:background="?android:attr/selectableItemBackground"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:focusable="false"
    android:clickable="false"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="8dp"
    android:paddingEnd="8dp">

    <!-- v6.27: wrap_content (was match_parent). The row's gravity="center"
         still centers this column against the Download button, and the
         column still centers its two labels — identical rendering at the
         default font scale, but correct at any font scale / measurement
         spec (a match_parent child of a wrap_content-height row is only
         accidentally correct and clips under some measurement passes). -->
    <LinearLayout
        android:gravity="center"
        android:orientation="vertical"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginEnd="8dp">

        <TextView
            android:textSize="14sp"
            android:id="@+id/txt_webview_name"
            android:scrollbars="none"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <TextView
            android:textSize="10sp"
            android:id="@+id/txt_webview_source"
            android:scrollbars="none"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_download"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/download"
        style="@style/Widget.Material3.Button.OutlinedButton" />
</LinearLayout>
